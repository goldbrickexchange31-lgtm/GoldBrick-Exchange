# Instructions for AI Agent

- NEVER delete or modify the file `public/og-image.png` or any file named `og-image.png`. It is a critical asset for the application's branding and metadata.
- If the file is missing, do not attempt to replace it unless explicitly requested by the user.
